import 'package:flutter/material.dart';

class SettingsPage extends StatelessWidget {
  // Capitalize class name
  const SettingsPage({super.key}); // Correct key handling

  @override
  Widget build(BuildContext context) {
    // Add semicolon
    return Container(
      color: Colors.blueGrey, // Add comma for color property
    );
  }
}
