import 'package:flutter/material.dart';

class NotificationsPage extends StatelessWidget {
  // Capitalize class name
  const NotificationsPage({super.key}); // Correct key handling

  @override
  Widget build(BuildContext context) {
    // Add semicolon
    return Container(
      color: Colors.yellowAccent, // Add comma for color property
    );
  }
}
