import 'package:flutter/material.dart';

class PaymentPage extends StatelessWidget {
  // Capitalize class name
  const PaymentPage({super.key}); // Correct key handling

  @override
  Widget build(BuildContext context) {
    // Add semicolon
    return Container(
      color: Colors.greenAccent, // Add comma for color property
    );
  }
}
